# vendored upstream JCB sources - regenerate with: php tools/cli.php fetch
/vendor-jcb/

# extracted fixture tree - regenerate from the tarball
/tests/fixtures/hello-world/

# vendored LionWeb spec artefacts - refetch from lionweb-io/specification
/vendor-lionweb/

# Phase 2 encoding experiment output (the experiment is settled)
/prototypes/

# packaged component
/build/*.zip

# Development Joomla site. Downloaded and installed locally; CI fetches its own.
# PHPStan scans it so `extends AdminModel` resolves to something.
/joomla/

# Working downloads
/.tmp/

# Dependencies
/vendor/
/node_modules/

# Cypress runs against a real site; the URL and login are per-machine.
# Copy cypress.env.json.dist and fill it in.
/cypress.env.json
/tests/cypress/screenshots/
/tests/cypress/videos/

# Test artefacts
/.phpunit.cache/
/.php-cs-fixer.cache

# IDE state
/.idea/
/.vscode/
/.iml

# Copied into the component at package time by tools/build.php; the source of
# truth is tests/fixtures/hello-world.
/src/administrator/components/com_jcbinout/data/hello-world/

# Artefacts the component derives on a site, never shipped.
/src/administrator/components/com_jcbinout/data/derived/
